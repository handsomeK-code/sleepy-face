<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
  <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

  <application>
    <receiver
      android:name=".AlarmRingingReceiver"
      android:exported="false" />

    <service
      android:name=".AlarmRingingService"
      android:exported="false"
      android:foregroundServiceType="mediaPlayback" />

    <receiver
      android:name=".SavedAlarmBootReceiver"
      android:exported="false">
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
      </intent-filter>
    </receiver>

    <service
      android:name=".SavedAlarmResyncTaskService"
      android:exported="false" />

    <!-- Default intent-filter priority (0) beats expo-notifications' own receiving
         service, which registers at a lower priority (see its AndroidManifest.xml).
         Firebase Messaging only ever delivers to one FirebaseMessagingService, so this
         one wins and is responsible for handling every push, not just alarm-activation
         ones. It delegates non-alarm-activation pushes to expo-notifications'
         FirebaseMessagingDelegate so existing behavior (channel config, tap handling,
         JS listeners, token refresh) is preserved; see AlarmActivationMessagingService. -->
    <service
      android:name=".AlarmActivationMessagingService"
      android:exported="false">
      <intent-filter>
        <action android:name="com.google.firebase.MESSAGING_EVENT" />
      </intent-filter>
    </service>
  </application>
</manifest>
