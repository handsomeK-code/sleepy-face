# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# UI screens must be responsive/flexible

Read `docs/ui_guidelines.md` before implementing or editing any screen. All screens must be responsive and flexible (ScrollView-safe, flex-based layout) — not hardcoded to one design mockup's fixed pixel dimensions.
